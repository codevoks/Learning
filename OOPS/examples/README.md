# C++ OOP Examples — Runnable Reference 🔥

> **Study material** (complete, runnable + commented). `problems/`-style stubs nahi — yeh canonical C++ mechanics demos. Pehle khud likhne ki koshish, phir compare.
> Build any: `g++ -std=c++17 file.cpp -o ex && ./ex`

| File | Concept (Prompt topic) |
|------|------------------------|
| `virtual_destructor.cpp` | why a base dtor must be virtual (4) |
| `rule_of_5.cpp` | copy/move ctor+assign + dtor, deep copy (5–10) |
| `shallow_vs_deep_copy.cpp` | shallow double-free vs deep copy (35) |
| `composition_vs_inheritance.cpp` | has-a vs is-a (36) |
| `object_slicing.cpp` | by-value base slices the derived (27) |
| `virtual_functions_vtable.cpp` | runtime dispatch via vtable (14,15,17) |
| `abstract_interface.cpp` | pure virtual / abstract / interface (18,19,20) |
| `function_overload_override.cpp` | overload vs override vs hide (25,26) |
| `dynamic_cast_rtti.cpp` | safe downcast + RTTI (30) |
| `static_members.cpp` | static data + member fns (22,23) |
| `friend_function.cpp` | friend operator<< (21) |
| `operator_overloading.cpp` | + , == , << on a value type (24) |
| `const_correctness.cpp` | const methods, const& , mutable (31) |
| `smart_pointers.cpp` | unique/shared/weak + cycle fix (32) |
| `raii.cpp` | resource = object lifetime (33,34) |

> Patterns (Strategy/Observer/etc.) + SOLID = sister folder `../../LLD/examples/`.
