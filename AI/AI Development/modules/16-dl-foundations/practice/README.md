# Module 16 — Assignments (DL Foundations)

> **Theory**: `../MODULE.md` · **Learnings**: `../NOTES.md` · **Doubt**: `@MODULE.md`
> A1–A3 offline (NumPy) — `python <file>.py` → `_check()` pass.

## Progress checklist

- [ ] A1 — `neuron_forward.py`
- [ ] A2 — `gradient_descent.py`
- [ ] A3 — `softmax_ce.py`
- [ ] Active recall → NOTES

## Setup

```bash
cd modules/16-dl-foundations/practice
python3 -m venv .venv && source .venv/bin/activate
pip install numpy
```

---

## A1 — Neuron forward

| | |
|---|---|
| **Theory §** | §2 |
| **File** | `neuron_forward.py` |
| **Kya** | Weighted sum + sigmoid |
| **Pass** | `python neuron_forward.py` → `_check()` asserts pass |

## A2 — Gradient descent

| | |
|---|---|
| **Theory §** | §3 |
| **File** | `gradient_descent.py` |
| **Kya** | Minimum tak converge |
| **Pass** | `python gradient_descent.py` → `_check()` asserts pass |

## A3 — Softmax + cross-entropy

| | |
|---|---|
| **Theory §** | §4 |
| **File** | `softmax_ce.py` |
| **Kya** | Stable softmax + CE loss |
| **Pass** | `python softmax_ce.py` → `_check()` asserts pass |

---

## Active recall (NOTES)

1. Activation non-linear na ho to network ki power?
2. Backprop + gradient descent loop 4 steps?
3. RNN/LSTM ki 2 seemायein transformers ne kaise todi?

**Stuck?** `@modules/16-dl-foundations/MODULE.md` + error paste.
