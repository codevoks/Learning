"""M2 — Fallback: primary 5xx → secondary provider. Complete TODOs."""

# TODO: async def call_with_fallback(messages, primary_fn, secondary_fn)
# TODO: simulate primary failure for testing


async def call_with_fallback(messages: list[dict], primary_fn, secondary_fn) -> dict:
    raise NotImplementedError
