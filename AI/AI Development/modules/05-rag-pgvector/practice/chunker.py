"""A1 — Document chunker with overlap. Complete TODOs."""

# TODO: def load_document(path: str) -> str
# TODO: def chunk_text(text: str, chunk_size: int = 500, overlap: int = 50) -> list[str]


def chunk_text(text: str, chunk_size: int = 500, overlap: int = 50) -> list[str]:
    raise NotImplementedError
