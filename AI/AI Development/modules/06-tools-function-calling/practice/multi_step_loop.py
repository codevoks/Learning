"""A3 — Multi-step tool loop. Complete TODOs."""

# Query example: "Search docs for refund policy, then summarize in 2 bullets"
# Needs: search_docs → then LLM summarize (or second tool)

# TODO: def agent_loop(user_message: str, max_steps: int = 5) -> str
# TODO: stop when no more tool_calls


def agent_loop(user_message: str, max_steps: int = 5) -> str:
    raise NotImplementedError
