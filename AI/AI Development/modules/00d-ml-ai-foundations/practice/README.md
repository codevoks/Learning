# Practice — Module 00d

1. Read theory in `../MODULE.md`
2. Complete TODOs below
3. A2 diagram on paper / Excalidraw — not in repo

## Setup

```bash
cd modules/00d-ml-ai-foundations/practice
python3 -m venv .venv
source .venv/bin/activate
pip install numpy
```

### Optional (per assignment)

```bash
pip install tensorflow          # A3 — or tensorflow-macos on Apple Silicon
pip install openai python-dotenv   # A4 option A
pip install sentence-transformers  # A4 option B (local, no API key)
```

## Run

```bash
python cosine_similarity.py
python tf_hello.py
python embeddings_stub.py
```

## A4

Pick **one** path in `embeddings_stub.py` — OpenAI API or local `sentence-transformers`.
