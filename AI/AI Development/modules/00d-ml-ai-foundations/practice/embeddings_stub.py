"""A4 — Embeddings assignment entry point.

Pick ONE file to complete (same pass criteria):

  embeddings_local.py  — recommended: sentence-transformers, no API key
  embeddings_api.py    — OpenAI text-embedding-3-small, needs OPENAI_API_KEY

Shared TODOs live in embeddings_common.py (best_pair).
"""

raise SystemExit(
    "Pick one:\n"
    "  python embeddings_local.py   # Option B (recommended)\n"
    "  python embeddings_api.py     # Option A (OpenAI)\n"
)
