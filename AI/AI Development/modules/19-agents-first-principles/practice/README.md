# Module 19 — Assignments (Agents from First Principles)

> **Theory**: `../MODULE.md` · **Learnings**: `../NOTES.md` · **Doubt**: `@MODULE.md`
> A1, A2 offline (fake llm) — `python <file>.py` → `_check()` pass.

## Progress checklist

- [ ] A1 — `tool_registry.py`
- [ ] A2 — `agent_loop.py`
- [ ] A3 — `COMPARE.md`
- [ ] Active recall → NOTES

## Setup

```bash
cd modules/19-agents-first-principles/practice
python3 -m venv .venv && source .venv/bin/activate
# A1, A2 sirf stdlib (fake llm se test). Real LLM se bhi try karo.
```

---

## A1 — Tool registry

| | |
|---|---|
| **Theory §** | §3 |
| **File** | `tool_registry.py` |
| **Kya** | register / list / dispatch + allowlist |
| **Pass** | `python tool_registry.py` → `_check()` asserts pass |

## A2 — Agent loop

| | |
|---|---|
| **Theory §** | §2, §4 |
| **File** | `agent_loop.py` |
| **Kya** | think→act→observe loop + stop conditions |
| **Pass** | `python agent_loop.py` → `_check()` asserts pass |

## A3 — Compare (written)

| | |
|---|---|
| **Theory §** | §6 |
| **File** | `COMPARE.md` |
| **Kya** | Scratch agent vs LangGraph |
| **Pass** | Self / coach review |

---

## Active recall (NOTES)

1. Agent = ? (ek line)
2. 4 stop conditions?
3. LangGraph tumhare loop ke upar kya add karta hai?

**Stuck?** `@modules/19-agents-first-principles/MODULE.md` + error paste.
